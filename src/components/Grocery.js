
const Grocery = () => {
  return (
    <div className="grocery">
      <h1>An example of a component where lazy loading can be implemented</h1>
      <p>
        Lets say that we want to integrate a grocery store inside our application. Then, we will delay the loading of grocery component or resources until they are actually needed.
      </p>
    </div>
  );
};

export default Grocery;